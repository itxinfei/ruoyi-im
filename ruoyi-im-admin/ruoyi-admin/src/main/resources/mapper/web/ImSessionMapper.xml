<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.mapper.ImSessionMapper">

    <resultMap type="com.ruoyi.web.domain.ImSession" id="ImSessionResult">
        <id property="id" column="id"/>
        <result property="type" column="type"/>
        <result property="name" column="name"/>
        <result property="avatar" column="avatar"/>
        <result property="peerId" column="target_id"/>
        <result property="lastMessageTime" column="last_message_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectImSessionVo">
        select id, type, target_id, name, avatar, last_message_id, last_message_time, create_time, update_time
        from im_conversation
    </sql>

    <select id="selectImSessionList" parameterType="com.ruoyi.web.domain.ImSession" resultMap="ImSessionResult">
        <include refid="selectImSessionVo"/>
        <where>
            is_deleted = 0
            <if test="type != null and type != ''"> and type = #{type} </if>
            <if test="peerId != null"> and target_id = #{peerId} </if>
            <if test="userId != null"> and target_id = #{userId} </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                and date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectImSessionById" parameterType="Long" resultMap="ImSessionResult">
        <include refid="selectImSessionVo"/>
        where id = #{id} and is_deleted = 0
    </select>

    <insert id="insertImSession" parameterType="com.ruoyi.web.domain.ImSession" useGeneratedKeys="true" keyProperty="id">
        insert into im_conversation (type, target_id, name, avatar, last_message_id, last_message_time, create_time, update_time)
        values (#{type}, #{peerId}, #{name}, #{avatar}, #{lastMessageId}, #{lastMessageTime}, now(), now())
    </insert>

    <update id="updateImSession" parameterType="com.ruoyi.web.domain.ImSession">
        update im_conversation
        <trim prefix="SET" suffixOverrides=",">
            <if test="type != null and type != ''">type = #{type},</if>
            <if test="name != null">name = #{name},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="peerId != null">target_id = #{peerId},</if>
            <if test="lastMessageId != null">last_message_id = #{lastMessageId},</if>
            <if test="lastMessageTime != null">last_message_time = #{lastMessageTime},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="deleteImSessionById" parameterType="Long">
        update im_conversation set is_deleted = 1, deleted_time = now() where id = #{id}
    </update>

    <update id="deleteImSessionByIds" parameterType="Long[]">
        update im_conversation set is_deleted = 1, deleted_time = now() where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="countOnlineSessions" resultType="java.lang.Integer">
        select count(*) from im_conversation where is_deleted = 0
    </select>

    <select id="selectUserActiveSessions" parameterType="Long" resultMap="ImSessionResult">
        <include refid="selectImSessionVo"/>
        where is_deleted = 0
        order by last_message_time desc
    </select>

    <update id="kickOutSession" parameterType="Long">
        update im_conversation set is_deleted = 1, deleted_time = now() where id = #{sessionId}
    </update>

    <!-- 查询总会话数 -->
    <select id="countTotalSessions" resultType="java.lang.Integer">
        select count(*) from im_conversation where is_deleted = 0
    </select>

    <!-- 查询私聊会话数 -->
    <select id="countPrivateSessions" resultType="java.lang.Integer">
        select count(*) from im_conversation where type = 'PRIVATE' and is_deleted = 0
    </select>

    <!-- 查询群聊会话数 -->
    <select id="countGroupSessions" resultType="java.lang.Integer">
        select count(*) from im_conversation where type = 'GROUP' and is_deleted = 0
    </select>

    <!-- 查询今日活跃会话数 -->
    <select id="countTodayActiveSessions" resultType="java.lang.Integer">
        select count(*) from im_conversation
        where date(last_message_time) = curdate() and is_deleted = 0
    </select>
</mapper>
