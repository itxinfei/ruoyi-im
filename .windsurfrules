# 🚀 Antigravity Agent Protocol - Zero Hallucination Mode (v4.1)

> **警告：您已进入极致防溃散环境。**
> 当您在此项目（RuoYi IM）中运行 AI 生成时，必须绝对服从以下红线规则。违规将导致代码被直接拒收。

## 1. 🛑 语法与基建红线 (Syntax Redlines)
- **后端 (Java)**: **绝对锁定 JDK 1.8**。
  - 触发熔断：代码中一旦出现 `var`、`record`、`switch ->` 语法、`"""` 文本块、`Optional.isEmpty()`、`Collection.toList()`，立刻判定为无效代码！
- **前端 (Vue)**: **前端代码绝对纯净 JS**。
  - 必须使用 `<script setup lang="js">`。
  - 触发熔断：一旦出现 TypeScript 的 `interface`、`type` 定义或泛型 `<T>`，立刻判定为无效代码！

## 2. 🏗️ 架构穿透与烂命名防火墙 (Architecture & Naming)
- **绝对三层隔离**：Controller -> Service -> Mapper。严禁 Controller 越权直接调用 Mapper。
- **Entity 绝对隔离**：禁止把 Entity 类（有 `@TableName`）暴露给 Controller。入参必须 `DTO`，出参必须 `VO`。
- **命名黑名单**：严禁使用脱离业务语境的毫无意义的变量名：`data`, `info`, `temp`, `obj`, `handle`。必须用确切含义词（如 `fileInfo`, `messageVO`）。
- **异常捕获制**：Service 层业务异常必须抛出 `BusinessException`。返回结果必须统一被 `AjaxResult` 包裹封装。

## 3. 🛡️ 零幻觉与防止越权建库 (Zero-Hallucination & SSOT)
- **禁止凭空脑补**：如果你不知道某个接口长什么样，或者没看过定义，**严禁猜测与 Mock 空数据**！请立即向用户输出 `[HALT]: 缺少上下文，请提供` 并停止生成。
- **前置扫描协议 (重中之重)**：在新建任何 Utils 类、配置类 (Config) 或是核心 Bean 之前，**你必须**在工作区全局搜索（Grep/Glob）是否已有存量代码。
- **合并原则**：如有相关配置类，必须注入并合并逻辑，**绝对禁止**随手新建包名不同的重名文件，以免导致 `BeanDefinition` 冲突引发项目崩塌。

## 4. 🎨 UI 像素级刚性准则 (UI/UX Strict Mode)
- **剥夺自由选色权**：严禁私自使用如 `red`, `#FF0000`、乱搭高饱和度颜色。必须且只能使用既定的 CSS Tokens（如品牌色）。
- **长效内存防泄漏**：所写的组件只要涉及 WebSocket (`mitt.on`)、定时器 (`setInterval`)，必须在 `onUnmounted` 提供清空销毁代码！
- **重绘卡顿防抖**：遇到状态高频更新（如消息推送），必须强制加入 `debounce/throttle`。

**== 以上规则为 AI 运行最高宪法。生成代码前必需自我比对。 ==**
