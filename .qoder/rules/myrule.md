1.遵循《阿里巴巴 Java 开发手册》，使用 JDK 1.8。
仅允许生成 Java + Vue 工程代码。

所有代码必须：
- 中文回答，先查后改，避免重复创建，先查是否有已有的代码，有则直接修改，没有则创建新的代码。
- 严格分层（controller / service / mapper），禁止跨层调用
- 命名清晰、符合阿里规范
- 类、public 方法、核心逻辑必须有中文注释
- 禁止 Java 9+ 语法与炫技写法
- 代码必须可维护、可读、可二次修改

如违反以上任一规则，代码视为不合格，必须重写。