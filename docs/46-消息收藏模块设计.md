# 46 消息收藏模块设计文档

> 用途：定义消息收藏、分类检索、跳转定位实现方案
> 更新时间：2026-04-01
> 优先级：P2

---

## 一、设计概述

### 1.1 职责边界
- **需求参考**：详见 [13-需求明细.md](13-需求明细.md) §7.2。
- **技术定位**：负责收藏夹的 CRUD 逻辑、分类标签管理及"从收藏跳转至历史消息"的 ID 定位算法。

---

## 二、后端实现 (JDK 1.8 规范)

### 2.1 收藏持久化逻辑

```java
@Service
@RequiredArgsConstructor
public class ImFavoriteServiceImpl implements ImFavoriteService {

    private final ImFavoriteMapper favoriteMapper;

    /**
     * 收藏消息
     * 遵循 JDK 1.8 规范
     */
    @Transactional(rollbackFor = Exception.class)
    public void addFavorite(FavoriteDTO favoriteDTO) {
        ImFavorite favorite = new ImFavorite();
        BeanUtils.copyProperties(favoriteDTO, favorite);
        favorite.setCreateTime(new Date());
        favoriteMapper.insert(favorite);
    }
}
```

---

## 三、前端实现 (纯 JS 规范)

### 3.1 收藏列表逻辑封装

```javascript
/**
 * 收藏列表逻辑封装 (纯 JS)
 */
import { ref } from 'vue';
import { favoriteApi } from '@/api/favorite';

export function useFavorite() {
  const favorites = ref([]);

  const loadFavorites = async (params) => {
    const response = await favoriteApi.list(params);
    favorites.value = response.list || [];
  };

  const deleteFavorite = async (id) => {
    await favoriteApi.remove(id);
    await loadFavorites();
  };

  return {
    favorites,
    loadFavorites,
    deleteFavorite
  };
}
```

---

## 四、UI 表现与对齐规范 (DingTalk Style)

### 4.1 收藏页面整体布局 (Favorites Layout)

- **页面背景**：`#F2F4F5`
- **左侧分类导航**：
  - 宽度：`200px`
  - 背景：`#FFFFFF`
  - 边框：右侧 `1px solid rgba(23, 26, 29, 0.08)`

- **右侧内容区**：
  - 背景：`#FFFFFF`
  - 内边距：`20px`
  - 最小宽度：`600px`

### 4.2 左侧分类导航 (Category Navigation)

#### 4.2.1 导航项规范

- **高度**：固定 `40px`
- **内边距**：`12px 16px`
- **圆角**：`4px`
- **图标尺寸**：`16x16`
- **图标与文字间距**：`8px`

#### 4.2.2 导航项状态

| 状态 | 背景 | 文字色 | 附加样式 |
|-----|------|-------|---------|
| **默认** | `transparent` | `#171A1D` | 无 |
| **悬停** | `rgba(23, 26, 29, 0.04)` | `#171A1D` | 无 |
| **选中** | `#E6F2FF` | `#277EFB` | 左侧 `3px` 蓝色竖条 `#277EFB` |

#### 4.2.3 默认分类

| 分类 | 图标 | 图标色 |
|-----|------|-------|
| 全部收藏 | `Star` | `#FA8C16` |
| 我收藏的 | `User` | `#277EFB` |
| 他人收藏的 | `Users` | `#ADB1B8` |

#### 4.2.4 自定义分类

- **添加分类**：底部固定按钮 `+ 新建分类`
- **分类管理**：右键分类名称弹出菜单
  - 重命名
  - 删除（需二次确认）

### 4.3 收藏列表 (Favorites List)

#### 4.3.1 列表项布局

- **高度**：自适应，最小 `80px`
- **内边距**：`16px`
- **背景**：`#FFFFFF`
- **边框**：底边 `1px solid rgba(23, 26, 29, 0.08)`
- **Hover**：背景 `rgba(23, 26, 29, 0.02)`

#### 4.3.2 收藏卡片内容

##### 头部信息行

- **高度**：`32px`
- **布局**：
  - 左侧：发送者头像 `24x24` + 昵称 `14px`
  - 间距：`8px`
  - 右侧：收藏时间 `12px`，`rgba(23, 26, 29, 0.4)`

##### 消息内容区

- **内边距**：`12px 0`
- **背景**：`#F5F5F5`
- **圆角**：`8px`

##### 文本消息

- **字号**：`14px`
- **颜色**：`#171A1D`
- **最大行数**：3 行
- **超出显示**：显示省略号 + `展开`

##### 图片消息

- **缩略图**：`80x60`
- **圆角**：`4px`
- **点击**：全屏预览

##### 文件消息

- **布局**：文件图标 `32x32` + 文件名 `14px` + 文件大小 `12px`
- **间距**：`8px`

##### 链接卡片

- **宽度**：`280px`
- **高度**：`80px`
- **布局**：标题 `14px` + 摘要 `12px` + 缩略图 `80x60`

#### 4.3.3 底部操作区

- **高度**：`32px`
- **布局**：
  - 左侧：分类标签 `12px`
  - 右侧：操作按钮

##### 分类标签

- **高度**：`20px`
- **内边距**：`0 8px`
- **圆角**：`4px`
- **背景**：`#E6F2FF`
- **文字**：`#277EFB`，`12px`

##### 操作按钮

| 操作 | 按钮样式 | 颜色 |
|-----|---------|------|
| **跳转** | 文字按钮 `12px` | `#277EFB` |
| **复制** | 文字按钮 `12px` | `#171A1D` |
| **删除** | 文字按钮 `12px` | `#FF4D4F` |

### 4.4 空状态 (Empty State)

#### 4.4.1 无收藏

- **图标**：灰色星形图标 `80x80`
- **颜色**：`#ADB1B8`
- **文字**：
  - 主标题：`18px`，`#171A1D`
  - 副标题：`14px`，`rgba(23, 26, 29, 0.4)`
- **内容**：
  - 主标题："还没有收藏的消息"
  - 副标题："在聊天中右键消息即可收藏"

### 4.5 收藏详情抽屉 (Favorite Detail Drawer)

#### 4.5.1 抽屉规格

- **宽度**：`400px`
- **高度**：`100vh`
- **背景**：`#FFFFFF`
- **阴影**：`-4px 0 12px rgba(0, 0, 0, 0.1)`

#### 4.5.2 头部

- **高度**：`64px`
- **内边距**：`16px 20px`
- **内容**：
  - 标题：`收藏详情`，`18px`，粗体
  - 关闭按钮：`24x24`

#### 4.5.3 内容区

- **内边距**：`20px`

##### 原始消息

- **背景**：`#F5F5F5`
- **圆角**：`8px`
- **内边距**：`16px`
- **内容**：完整消息内容

##### 收藏信息

- **间距**：`16px`
- **内容**：
  - 收藏者：头像 `32x32` + 昵称 `14px`
  - 收藏时间：`12px`，`rgba(23, 26, 29, 0.4)`
  - 所属会话：`14px`，`#277EFB`

##### 分类管理

- **标题**：`分类`，`14px`，粗体
- **间距**：`8px`
- **分类标签**：可多选，点击切换

#### 4.5.4 底部操作

- **高度**：`80px`
- **内边距**：`16px 20px`
- **背景**：`#FFFFFF`
- **边框**：顶部 `1px solid rgba(23, 26, 29, 0.08)`

##### 操作按钮

| 操作 | 按钮样式 | 宽度 | 说明 |
|-----|---------|------|------|
| **跳转到聊天** | 主按钮 `40px` 高 | `100%` | 跳转到原消息位置 |
| **取消收藏** | 文字按钮 | `80px` | 红色文字 |

### 4.6 跳转定位 (Jump to Location)

#### 4.6.1 跳转动画

- **动画方式**：淡出 `200ms` → 路由跳转 → 淡入 `200ms`
- **目标定位**：滚动到原消息位置
- **平滑滚动**：`300ms`

#### 4.6.2 目标消息高亮

- **高亮时长**：`2s`
- **高亮背景**：`#E6F2FF`
- **高亮动画**：
  - 开始：背景色 `#E6F2FF`
  - 结束：背景色 `transparent`
  - 过渡：`2s` 渐变

#### 4.6.3 定位提示

- **位置**：页面顶部
- **内容**：`已定位到收藏的消息`
- **样式**：
  - 高度：`48px`
  - 背景：`#E6F2FF`
  - 文字：`14px`，`#277EFB`
  - 自动消失：`3s`

---

## 五、约束与限制 (Constraints)

### 5.1 收藏容量

- **单人上限**：1000 条（C-013）
- **单项大小**：不超过 10MB（C-013）

### 5.2 收藏分类

- **最大分类数**：50 个
- **分类名称长度**：最多 20 字符
- **分类名称**：不能重复

### 5.3 收藏保留

- **保留策略**：与消息存储周期一致，保留 90 天（C-016）

---

## 六、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-04-01 | 统一品牌色为 #277EFB；补充收藏页面布局、分类导航、收藏列表样式、收藏详情抽屉、跳转定位规范、约束限制 | AI |
| 2026-03-21 | 彻底肃清 TypeScript 语法，重构为纯 JS 逻辑 | AI |