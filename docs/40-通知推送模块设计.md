# 40 通知推送模块设计文档

> 用途：定义消息通知、桌面推送、免打扰技术实现方案
> **更新时间**：2026-04-17
> 优先级：P1

---

## 一、设计概述

### 1.1 职责边界
- **业务需求**：详见 [13-需求明细.md](13-需求明细.md) §17。
- **技术定位**：本模块负责 Web Notification API 的封装、离线通知策略以及免打扰时段的逻辑判定。

---

## 二、前端实现 (纯 JS 规范)

### 2.1 通知管理服务封装

```javascript
/**
 * 通知管理服务 (NotificationService)
 * 强制要求：禁止使用 TypeScript，遵循纯 JS 规范
 */
export const notificationService = {
  permission: 'default',
  audioContext: null,
  notificationSound: null,

  /**
   * 请求通知权限
   */
  async requestPermission() {
    if (!('Notification' in window)) {
      return false;
    }
    const permission = await Notification.requestPermission();
    this.permission = permission;
    return permission === 'granted';
  },

  /**
   * 发送桌面通知
   */
  async notify(options, conversationId) {
    // 1. 检查权限
    if (this.permission !== 'granted') {
      const granted = await this.requestPermission();
      if (!granted) return null;
    }

    // 2. 检查免打扰 (armor logic)
    if (this.isInMuteTime()) {
      return null;
    }

    // 3. 窗口聚焦时不发送
    if (document.hasFocus()) {
      return null;
    }

    // 4. 创建通知实例
    const noticePayload = options.payload || {};
    const notification = new Notification(options.title, {
      body: options.body || '您有新消息',
      icon: options.icon || '/logo.png',
      tag: options.tag,
      data: noticePayload
    });

    // 监听点击
    notification.onclick = () => {
      this.processNotificationClick(notification, noticePayload);
    };

    return notification;
  },

  /**
   * 检查是否在免打扰时间
   */
  isInMuteTime() {
    const now = new Date();
    const currentTime = now.getHours() * 60 + now.getMinutes();
    // 假设从 store 获取配置 (C-003 相关)
    const startTime = 22 * 60; // 22:00
    const endTime = 8 * 60;   // 08:00
    
    if (startTime < endTime) {
      return currentTime >= startTime && currentTime <= endTime;
    }
    return currentTime >= startTime || currentTime <= endTime;
  },

  /**
   * 处理通知点击与跳转
   */
  processNotificationClick(notification, payload) {
    notification.close();
    window.focus();

    if (payload.type === 'message') {
      // 触发全局事件进行路由跳转
      window.dispatchEvent(new CustomEvent('im-nav', { 
        detail: { conversationId: payload.conversationId } 
      }));
    }
  }
};
```

---

## 三、后端实现 (JDK 1.8 规范)

### 3.1 通知异步分发 (EventBus)

```java
@Service
@RequiredArgsConstructor
public class ImNotificationServiceImpl implements ImNotificationService {

    private final SimpMessagingTemplate messagingTemplate;

    /**
     * 异步发送系统通知
     * 遵循 JDK 1.8 规范，严禁 var
     */
    @Async("noticeExecutor")
    public void sendSystemNotice(Long userId, String content) {
        NotificationVO noticeVO = new NotificationVO();
        noticeVO.setUserId(userId);
        noticeVO.setContent(content);
        noticeVO.setCreateTime(System.currentTimeMillis());
        
        // 推送 WebSocket 帧 (Doc-34)
        messagingTemplate.convertAndSendToUser(
            userId.toString(), 
            "/queue/notice", 
            noticeVO
        );
    }
}
```

---

## 四、UI 表现与对齐规范 (DingTalk Style)

### 4.1 桌面通知 (Desktop Notification)

#### 4.1.1 通知样式

- **宽度**：`320px`
- **高度**：自适应，最大 `200px`
- **背景**：`#FFFFFF`
- **圆角**：`12px`
- **阴影**：`0 8px 24px rgba(0, 0, 0, 0.15)`
- **边框**：`1px solid rgba(0, 0, 0, 0.08)`

#### 4.1.2 通知内容布局

- **内边距**：`16px`
- **布局**：
  - 顶部：图标 `32x32` + 标题 `14px` + 关闭按钮 `16x16`
  - 中部：正文 `14px`
  - 底部：操作按钮

#### 4.1.3 消息通知内容

| 元素 | 位置 | 尺寸 | 说明 |
|-----|------|------|------|
| **图标** | 左上角 | `32x32` | 发送者头像，圆角 `4px` |
| **标题** | 图标右侧 | `14px` | 发送者昵称 |
| **正文** | 下方 | `14px` | 消息摘要，最多 2 行 |
| **时间** | 右上角 | `12px` | 接收时间，灰色 |

#### 4.1.4 操作按钮

- **高度**：`32px`
- **内边距**：`0 16px`
- **背景**：`#F5F5F5`
- **文字**：`14px`，`#171A1D`
- **圆角**：`4px`
- **Hover**：背景 `#E5E7EB`

| 按钮 | 文字 | 说明 |
|-----|------|------|
| **回复** | 回复 | 点击跳转到会话 |
| **关闭** | 关闭 | 关闭通知 |

#### 4.1.5 通知行为

- **出现动画**：从右上角滑入，`300ms`
- **消失动画**：淡出 + 向上移动，`250ms`
- **停留时间**：`5s`
- **Hover**：暂停计时
- **点击**：跳转到对应会话，关闭通知

### 4.2 应用内消息提示 (In-App Toast)

#### 4.2.1 通知卡片样式

- **位置**：屏幕右上角，距离顶部 `20px`，距离右侧 `20px`
- **宽度**：`360px`
- **最小高度**：`80px`
- **背景**：`#FFFFFF`
- **圆角**：`8px`
- **阴影**：`0 4px 16px rgba(0, 0, 0, 0.1)`
- **边框**：`1px solid rgba(0, 0, 0, 0.08)`

#### 4.2.2 通知卡片布局

- **内边距**：`12px 16px`
- **布局**：
  - 左侧：发送者头像 `40x40`，圆角 `4px`
  - 右侧：发送者信息 + 消息摘要

#### 4.2.3 发送者信息

- **布局**：
  - 昵称：`14px`，粗体，`#171A1D`
  - 时间：`12px`，`rgba(23, 26, 29, 0.4)`
  - 间距：`8px`

#### 4.2.4 消息摘要

- **字号**：`14px`
- **颜色**：`#171A1D`
- **最大行数**：2 行
- **超出显示**：显示省略号

#### 4.2.5 通知堆叠

- **最大显示数**：3 条
- **间距**：`8px`
- **新通知出现**：旧通知下移
- **自动消失**：`5s`

### 4.3 免打扰状态表现 (Mute Status)

#### 4.3.1 会话列表免打扰标识

- **位置**：会话项右侧
- **图标**：铃铛图标 `16x16`，带斜线
- **颜色**：`#ADB1B8`
- **背景**：无

#### 4.3.2 未读角标处理

- **显示方式**：
  - 正常会话：显示数字角标 `16x16`，红色 `#FF4D4F`
  - 免打扰会话：仅显示红点 `8x8`，红色 `#FF4D4F`
- **位置**：头像右上角
- **圆角**：`50%`（正圆）

#### 4.3.3 免打扰时段设置

- **设置入口**：设置页面 → 通知设置
- **开关**：`开启免打扰`，Element Plus `small` 开关
- **时间段选择**：
  - 开始时间：下拉选择，`00:00` - `23:59`
  - 结束时间：下拉选择，`00:00` - `23:59`
- **默认值**：`22:00 - 08:00`

### 4.4 DING消息通知 (DING Notification)

#### 4.4.1 强提醒通知样式

- **宽度**：`480px`
- **高度**：`120px`
- **背景**：`#FFF7E6`（橙色背景）
- **圆角**：`12px`
- **阴影**：`0 8px 24px rgba(250, 140, 22, 0.3)`
- **边框**：`2px solid #FA8C16`

#### 4.4.2 DING通知内容

| 元素 | 位置 | 尺寸 | 说明 |
|-----|------|------|------|
| **图标** | 左侧 | `48x48` | 铃铛图标，橙色 `#FA8C16` |
| **标题** | 图标右侧 | `18px`，粗体 | `DING消息` |
| **正文** | 下方 | `14px` | DING消息内容 |
| **按钮** | 右侧 | `32x32` | 确认按钮 |

#### 4.4.3 DING通知行为

- **出现动画**：从右上角滑入，`300ms`，带弹性效果
- **消失动画**：仅用户点击确认后消失
- **声音**：播放特殊提示音
- **震动**：浏览器震动 API（如支持）

---

## 五、通知权限管理 (Notification Permission)

### 5.1 权限状态

| 状态 | 说明 | 行为 |
|-----|------|------|
| **granted** | 已授权 | 正常发送通知 |
| **denied** | 已拒绝 | 不发送通知，提示用户开启 |
| **default** | 未授权 | 请求授权 |

### 5.2 权限请求弹窗

- **触发**：首次发送通知时
- **弹窗内容**：
  - 标题：`开启消息通知`
  - 说明：`为了及时接收消息，请允许发送通知`
  - 按钮：`开启` / `暂不`

### 5.3 权限设置入口

- **设置页面**：设置 → 通知设置
- **开关**：`桌面通知`，Element Plus `small` 开关
- **状态显示**：当前权限状态

---

## 六、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-04-01 | 统一品牌色为 #277EFB；补充桌面通知样式、应用内提示详细布局、免打扰状态规范、DING消息通知样式、通知权限管理 | AI |
| 2026-03-21 | 肃清 TypeScript 污染，重构为纯 JS 逻辑，对齐 JDK 1.8 | AI |