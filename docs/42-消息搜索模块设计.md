# 42 消息搜索模块设计文档

> 用途：定义全局搜索、会话内搜索实现方案
> **更新时间**：2026-04-17
> 优先级：P1

---

## 一、设计概述

### 1.1 职责边界
- **需求参考**：详见 [13-需求明细.md](13-需求明细.md) §6。
- **技术定位**：负责基于数据库全文索引 (NGRAM) 的内容检索、多维搜索结果聚合及关键词高亮逻辑实现。

---

## 二、后端实现 (JDK 1.8 规范)

### 2.1 全文搜索逻辑 (MySQL 5.7)

```java
@Service
@RequiredArgsConstructor
public class ImSearchServiceImpl implements ImSearchService {

    private final ImMessageMapper messageMapper;

    /**
     * 搜索消息内容 (高亮处理)
     * 遵循 JDK 1.8，严禁 var
     */
    public List<MessageVO> searchMessages(String keyword, Long conversationId) {
        LambdaQueryWrapper<ImMessage> wrapper = Wrappers.lambdaQuery();
        wrapper.eq(ImMessage::getConversationId, conversationId);
        // 使用 MATCH AGAINST 实现全文检索
        wrapper.apply("MATCH(content) AGAINST({0} IN BOOLEAN MODE)", keyword);
        
        List<ImMessage> list = messageMapper.selectList(wrapper);
        return convertToVO(list, keyword);
    }
}
```

---

## 三、前端实现 (纯 JS 规范)

### 3.1 搜索防抖与跳转逻辑

```javascript
/**
 * 搜索逻辑封装 (纯 JS)
 */
import { ref } from 'vue';
import { searchApi } from '@/api/search';

export function useSearch() {
  const keyword = ref('');
  const results = ref([]);
  let timer = null;

  const processSearchInput = () => {
    if (timer) clearTimeout(timer);
    timer = setTimeout(async () => {
      if (!keyword.value) return;
      const response = await searchApi.global(keyword.value);
      results.value = response.data || [];
    }, 300);
  };

  const goToResult = (item) => {
    // 根据 item.type 进行路由跳转逻辑
  };

  return {
    keyword,
    results,
    processSearchInput,
    goToResult
  };
}
```

---

## 四、UI 表现与对齐规范 (DingTalk Style)

### 4.1 搜索输入框 (Search Input)

#### 4.1.1 会话列表搜索框

- **位置**：L2 会话列表顶部
- **尺寸**：高度 `32px`，宽度 `100%`（减去左右内边距）
- **内边距**：`0 12px`
- **背景色**：`rgba(23, 26, 29, 0.04)`
- **圆角**：`4px`
- **边框**：`1px solid transparent`

#### 4.1.2 输入框状态

| 状态 | 背景色 | 边框 | 阴影 | 说明 |
|-----|-------|------|------|------|
| **默认** | `rgba(23, 26, 29, 0.04)` | `1px solid transparent` | 无 | 灰色背景 |
| **Focus** | `#FFFFFF` | `1px solid #277EFB` | `0 0 0 2px rgba(0, 137, 255, 0.1)` | 蓝色边框 + 外发光 |
| **Hover** | `rgba(23, 26, 29, 0.06)` | `1px solid transparent` | 无 | 深色背景 |

#### 4.1.3 图标与按钮

- **搜索图标**：
  - 位置：左侧 `12px`
  - 尺寸：`16x16`
  - 颜色：`#ADB1B8`

- **清除按钮**：
  - 位置：右侧 `8px`
  - 尺寸：`16x16`
  - 图标：`×`
  - 颜色：`#ADB1B8`
  - Hover：`#171A1D`
  - 显示条件：输入内容非空时显示

### 4.2 全局搜索弹窗 (Global Search Dialog)

#### 4.2.1 弹窗尺寸与位置

- **尺寸**：宽度 `800px`，高度 `600px`（最大高度 `80vh`）
- **位置**：屏幕中央，水平垂直居中
- **背景**：`#FFFFFF`
- **圆角**：`12px`
- **阴影**：`0 20px 60px rgba(0, 0, 0, 0.15)`
- **遮罩**：全屏半透明遮罩 `rgba(0, 0, 0, 0.4)`

#### 4.2.2 弹窗布局

```
┌─────────────────────────────────────────────────────────────┐
│  搜索输入框                                          [×]  │  ← 头部（高度 64px）
├─────────────────────────────────────────────────────────────┤
│  [全部] [消息] [联系人] [群组] [文件]                        │  ← 分类 Tab（高度 48px）
├─────────────────────────────────────────────────────────────┤
│  搜索结果列表                                              │  ← 内容区（滚动）
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 搜索结果项                                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 4.2.3 顶部搜索区

- **高度**：`64px`
- **内边距**`16px 24px`
- **搜索框**：
  - 高度：`40px`
  - 背景：`#F5F5F5`
  - 圆角：`20px`
  - 内边距：`0 16px`
  - 图标：左侧 `16x16`，`#ADB1B8`

#### 4.2.4 分类 Tab (Category Tabs)

- **高度**：`48px`
- **内边距**：`0 24px`
- **Tab 项**：
  - 高度：`32px`
  - 内边距：`0 16px`
  - 圆角：`4px`
  - 字号：`14px`
  - 间距：`8px`

| 状态 | 背景 | 文字色 | 边框 | 说明 |
|-----|------|-------|------|------|
| **默认** | `transparent` | `#171A1D` | 无 | 默认态 |
| **Hover** | `rgba(23, 26, 29, 0.04)` | `#171A1D` | 无 | 悬浮态 |
| **选中** | `#E6F2FF` | `#277EFB` | 无 | 激活态 |

#### 4.2.5 搜索结果列表

- **背景**：`#FFFFFF`
- **内边距**：`0 24px 24px 24px`
- **最大高度**：`450px`（超出滚动）
- **分组标题**：
  - 高度：`40px`
  - 字号：`14px`，粗体
  - 颜色：`#171A1D`
  - 上边距：`16px`
  - 下边距：`8px`

### 4.3 搜索结果卡片 (Search Result Card)

#### 4.3.1 消息搜索结果

- **高度**：自适应，最小 `64px`
- **内边距**：`12px 16px`
- **背景**：`#FFFFFF`
- **边框**：底边 `1px solid rgba(23, 26, 29, 0.08)`
- **Hover**：背景 `rgba(23, 26, 29, 0.02)`

| 元素 | 位置 | 尺寸 | 说明 |
|-----|------|------|------|
| **头像** | 左侧 | `40x40` | 圆角 `4px` |
| **用户信息** | 头像右侧 | - | 昵称 `14px` + 时间 `12px` |
| **消息内容** | 用户信息下方 | - | `14px`，最多两行 |
| **会话名称** | 右侧 | - | `12px`，灰色 |

#### 4.3.2 联系人搜索结果

- **高度**：`64px`
- **内边距**：`12px 16px`
- **布局**：
  - 左侧：头像 `40x40`，圆角 `4px`
  - 中间：昵称 `14px` + 部门 `12px`
  - 右侧：[发消息] 按钮 `24px` 高

#### 4.3.3 群组搜索结果

- **高度**：`64px`
- **内边距**：`12px 16px`
- **布局**：
  - 左侧：群头像 `40x40`，圆角 `8px`
  - 中间：群名称 `14px` + 成员数 `12px`
  - 右侧：[进入] 按钮 `24px` 高

#### 4.3.4 文件搜索结果

- **高度**：`72px`
- **内边距**：`12px 16px`
- **布局**：
  - 左侧：文件图标 `40x40`
  - 中间：文件名 `14px` + 文件大小 `12px` + 上传时间 `12px`
  - 右侧：[下载] 按钮 `24px` 高

### 4.4 关键词高亮 (Keyword Highlight)

#### 4.4.1 高亮样式

- **颜色**：`--dt-brand-color`（品牌蓝 #277EFB）
- **背景**：`--dt-brand-bg`（#E6F2FF）
- **字体**：`14px`，粗体
- **CSS 类名**：`.search-highlight`

```css
.search-highlight {
  color: var(--dt-brand-color);
  background: var(--dt-brand-bg);
  font-weight: 600;
  padding: 0 2px;
  border-radius: var(--dt-radius-xs);
}
```

#### 4.4.2 高亮规则

- **匹配位置**：消息内容中的关键词
- **匹配方式**：后端切词标记，前端统一应用高亮样式
- **最大匹配数**：每条消息最多高亮 3 处
- **字符限制**：单次高亮不超过 20 个字符

### 4.5 空状态 (Empty State)

#### 4.5.1 无搜索结果

- **图标**：灰色搜索图标 `80x80`
- **颜色**：`#ADB1B8`
- **文字**：
  - 主标题：`18px`，`#171A1D`
  - 副标题：`14px`，`rgba(23, 26, 29, 0.4)`
- **内容**：
  - 主标题："未搜索到相关内容"
  - 副标题："换个关键词试试"

#### 4.5.2 搜索建议

- **位置**：搜索框下方
- **内容**：历史搜索记录 + 热门搜索
- **布局**：标签形式，间距 `8px`

| 标签类型 | 背景 | 文字色 | 圆角 | 内边距 |
|---------|------|-------|------|--------|
| **历史搜索** | `#F5F5F5` | `#171A1D` | `4px` | `4px 12px` |
| **热门搜索** | `#E6F2FF` | `#277EFB` | `4px` | `4px 12px` |
| **Hover** | `#E5E7EB` / `#D6E4FF` | - | - | - |

### 4.6 会话内搜索 (In-Conversation Search)

#### 4.6.1 搜索框

- **位置**：聊天窗口顶部
- **尺寸**：高度 `36px`，宽度 `240px`
- **背景**：`#F5F5F5`
- **圆角**：`18px`
- **内边距**：`0 16px`

#### 4.6.2 搜索结果导航

- **位置**：搜索框下方
- **内容**：`找到 X 条结果`
- **按钮**：[上一条] [下一条]
- **按钮样式**：
  - 高度：`28px`
  - 内边距：`0 12px`
  - 背景：`#F5F5F5`
  - Hover：`#E5E7EB`

#### 4.6.3 结果定位

- **方式**：点击结果后滚动到对应消息位置
- **动画**：平滑滚动，`300ms`
- **高亮**：目标消息高亮显示 `2s`，背景 `#E6F2FF`

---

## 五、交互反馈 (Interaction Feedback)

### 5.1 搜索加载状态

- **加载动画**：搜索框右侧显示 Loading 图标 `16x16`
- **加载文字**：搜索中...
- **位置**：搜索结果列表顶部

### 5.2 搜索结果跳转

- **点击结果**：跳转到对应位置
- **动画**：淡出 `200ms` → 跳转 → 淡入 `200ms`
- **焦点**：聚焦到目标消息

### 5.3 快捷键支持

| 快捷键 | 功能 | 说明 |
|-------|------|------|
| `Ctrl + F` | 打开全局搜索 | 弹出搜索弹窗 |
| `Ctrl + K` | 快速搜索 | L2 搜索框聚焦 |
| `Esc` | 关闭搜索弹窗 | 关闭搜索窗口 |
| `↑ / ↓` | 导航搜索结果 | 在结果列表中上下移动 |
| `Enter` | 跳转到结果 | 打开当前选中项 |

---

## 六、性能优化 (Performance)

### 6.1 搜索防抖

- **防抖时间**：`300ms`
- **实现方式**：防抖函数，清除之前的定时器

### 6.2 结果分页

- **每页数量**：20 条
- **加载更多**：滚动到底部自动加载下一页
- **显示加载中**：底部显示"加载更多..."提示

### 6.3 缓存策略

- **缓存时间**：5 分钟
- **缓存 key**：`search:${keyword}:${category}`
- **缓存失效**：新消息到达时清除相关缓存

---

## 七、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-04-01 | 统一品牌色为 #277EFB；补充搜索输入框状态规范、全局搜索弹窗布局、搜索结果卡片样式、关键词高亮规范、空状态、快捷键支持、性能优化 | AI |
| 2026-03-21 | 肃清 TypeScript 污染，重构为纯 JS 防抖逻辑 | AI |
