# 41 安全与权限模块设计文档

> 用途：定义登录认证、权限控制、审计日志实现方案
> 更新时间：2026-03-21
> 优先级：P1

---

## 一、设计概述

### 1.1 职责边界
- **认证**：基于 JWT 的无状态认证逻辑。
- **授权**：基于 RBAC 模型的权限校验（RequireRole/RequirePermission）。
- **内容安全**：敏感词过滤（AC 自动机）与消息掩码。

---

## 二、后端实现 (JDK 1.8 规范)

### 2.1 权限拦截注解

```java
@Target({ElementType.TYPE, ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface RequirePermission {
    String[] value();
}
```

### 2.2 JWT 解析逻辑

```java
@Service
@RequiredArgsConstructor
public class JwtTokenService {

    /**
     * 获取用户权限列表
     * 遵循 JDK 1.8，严禁 var
     */
    public List<String> getPermissions(String token) {
        Claims claims = parseClaims(token);
        Object permissionsObj = claims.get("permissions");
        if (Objects.nonNull(permissionsObj)) {
            return (List<String>) permissionsObj;
        }
        return new ArrayList<>();
    }
}
```

---

## 三、前端实现 (纯 JS 规范)

### 3.1 权限指令封装

```javascript
/**
 * 权限校验逻辑 (纯 JS)
 * 强制要求：严禁使用 TypeScript
 */
export function hasPermission(permission) {
  const userPermissions = []; // 从 store 获取
  if (!permission) return true;
  
  if (Array.isArray(permission)) {
    return permission.some(p => userPermissions.includes(p));
  }
  return userPermissions.includes(permission);
}
```

---

## 四、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-03-21 | 肃清 TypeScript 残余，规范 JDK 1.8 权限拦截逻辑 | AI |
