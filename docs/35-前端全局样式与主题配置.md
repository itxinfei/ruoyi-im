# 35 前端全局样式与主题配置 (Theme & Styles)

> **用途**：统一内网环境下 UI 的视觉基因，确保所有样式基于项目实际的 CSS 变量和设计令牌（design‑tokens），防止硬编码和错误的 Tailwind 引用。

---

## 1. 样式体系概述

项目采用 **CSS 变量 + SCSS 设计令牌** 的方式管理颜色、间距、圆角等全局视觉属性。所有组件样式应通过 `var(--xxx)` 或 SCSS 变量引用，避免直接写死颜色或尺寸。

- **设计令牌文件**：`src/styles/design-tokens.scss` 定义了颜色、字体、阴影等变量，并在 `:root` 中导出为 CSS 变量。
- **全局样式入口**：`src/styles/global.scss` 包含基础重置、Element Plus 覆盖以及自定义实用类（如 `.flex-center`）。
- **Tailwind**：项目中仅保留 Tailwind 的 **工具类指令**（如 `@apply`），不作为直接的 UI 类库使用，文档中不应出现 `bg-primary-sidebar` 等 Tailwind 类的硬编码示例。

---

## 2. 钉钉级视觉契约 (Visual Contract)

在 `src/assets/styles/variables.css` 中强制声明以下变量，所有组件开发必须以此为准：

```css
:root {
  /* 品牌色 - 官方钉钉蓝 (DingTalk Official Blue) */
  --dt-brand-color: #277EFB;           /* 品牌主色 (钉钉官方标准) */
  --dt-brand-hover: #1169E0;
  --dt-brand-active: #0656C6;
  --dt-brand-bg: #E6F2FF;              /* 浅蓝色背景 */

  /* 中性色 - 信息分层 (DingDesign Neutrals) */
  --dt-text-main: #171A1D;             /* 一级文本 (90% opacity) */
  --dt-text-desc: rgba(23, 26, 29, 0.6); /* 二级文本 (60% opacity) */
  --dt-text-hint: rgba(23, 26, 29, 0.4); /* 三级文本 (40% opacity) */
  --dt-text-white: #FFFFFF;
  --dt-border-color: rgba(23, 26, 29, 0.08); /* 标准分割线 */
  --dt-bg-body: #F2F4F5;               /* 页面背景底色 */
  --dt-bg-container: #FFFFFF;          /* 容器/卡片背景 */
  --dt-bg-hover: rgba(23, 26, 29, 0.04); /* 列表项 Hover 态 */
  --dt-bg-selected: #E6F2FF;           /* 选中态/激活项背景 */

  /* 侧边栏专属 (Windows 桌面端深色基因) */
  --dt-sb-bg: #2B2D33;                 /* Windows 版侧边栏背景 */
  --dt-sb-icon: #ADB1B8;
  --dt-sb-icon-active: #FFFFFF;

  /* 布局与间距 (8px Step System) */
  --dt-radius-s: 4px;                  /* 按钮/输入框圆角 */
  --dt-radius-m: 8px;                  /* 气泡/弹窗/卡片圆角 */
  --dt-spacing-xs: 4px;
  --dt-spacing-s: 8px;
  --dt-spacing-m: 16px;
  --dt-spacing-l: 24px;
  --dt-line-width: 1px;
}

/* 暗色模式完整色板 (Dark Mode Palette) */
[data-theme="dark"] {
  /* 品牌色 - 保持不变 */
  --dt-brand-color: #277EFB;
  --dt-brand-hover: #1169E0;
  --dt-brand-active: #0656C6;
  --dt-brand-bg: rgba(0, 137, 255, 0.15);

  /* 中性色 - 暗色模式适配 */
  --dt-text-main: #F9FAFB;             /* 一级文本 (亮色) */
  --dt-text-desc: rgba(249, 250, 251, 0.6); /* 二级文本 */
  --dt-text-hint: rgba(249, 250, 251, 0.4); /* 三级文本 */
  --dt-text-white: #171A1D;            /* 反转：深色文字用于浅色背景 */
  --dt-border-color: rgba(255, 255, 255, 0.08); /* 标准分割线 */
  --dt-bg-body: #1F2937;               /* 页面背景底色 (深灰) */
  --dt-bg-container: #374151;          /* 容器/卡片背景 */
  --dt-bg-hover: rgba(255, 255, 255, 0.04); /* 列表项 Hover 态 */
  --dt-bg-selected: rgba(0, 137, 255, 0.2); /* 选中态背景 */

  /* 侧边栏 - 暗色模式更深 */
  --dt-sb-bg: #111827;                 /* 更深的侧边栏背景 */
  --dt-sb-icon: #9CA3AF;
  --dt-sb-icon-active: #FFFFFF;

  /* 消息气泡 - 暗色模式适配 */
  --dt-bubble-self: #277EFB;           /* 发送方气泡保持品牌蓝 */
  --dt-bubble-other: #374151;          /* 接收方气泡使用深灰 */
}
```

---

## 3. 组件级样式指南 (Implementation Logic)

为了让 UI 不产生“廉价感”，AI 在编写 Vue 组件时必须遵循以下逻辑：

### 3.1 会话列表项 (Session Item)
- **高度**：固定 `64px`（极致紧凑）。
- **背景**：默认透明，Hover 使用 `var(--dt-bg-hover)`，选中时使用 `var(--dt-bg-selected)` 且左侧带一条 `3px` 蓝边。
- **摘要文字**：必须限制为 `ellipsis` (单行溢出省略)，使用 `var(--dt-text-desc)`。

### 3.2 消息气泡 (Message Bubble)
- **间距**：同侧气泡间距 `4px`，异侧 `16px`。
- **头像**：圆角固定 `4px`（不要用圆形，圆形是移动端基因），尺寸 `36x36`。
- **文字**：行高 `1.5`，字体大小 `14px`。

### 3.3 全局边框
- 所有卡片和面板的边框必须是 `var(--dt-line-width) solid var(--dt-border-color)`。
- 禁止使用 `box-shadow` 做大面积投影，仅允许在悬浮菜单使用极轻微阴影。


---

## 3. 本地图标索引 (Local Icons)

项目统一使用本地 SVG 图标或 Element Plus 内置图标，禁止外部 CDN。通过 `<svg-icon name="..." />` 组件引用。

| 图标 ID | 对应功能 | 来源 |
| :--- | :--- | :--- |
| `icon-msg` | 消息模块 | Element Plus `ChatDotRound` |
| `icon-contact` | 通讯录 | Element Plus `User` |
| `icon-workbench` | 工作台 | Element Plus `Menu` |
| `icon-search` | 全局搜索 | Element Plus `Search` |
| `icon-file-pdf` | PDF 文件 | 本地 `src/assets/icons/file-pdf.svg` |
| `icon-file-word` | Word 文件 | 本地 `src/assets/icons/file-word.svg` |

### 调用方式
```vue
<svg-icon name="icon-msg" class="text-primary" />
```

---

## 4. 全局实用类 (Utility Classes)

为配合 CSS 变量，项目提供了一套基于 **Tailwind @apply** 的实用类，仅用于快速布局，**不**用于颜色或尺寸硬编码。

```scss
/* src/styles/utilities.scss */
@layer utilities {
  .flex-center { @apply flex items-center justify-center; }
  .text-primary { color: var(--dt-brand-color); }
  .bg-primary { background-color: var(--dt-brand-color); }
  .rounded-bubble { border-radius: var(--dt-radius-m); }
}
```

> **变量命名规范**：
> - `--dt-*`：设计令牌（Design Token），定义基础视觉基因（颜色、间距、圆角）
> - `--im-*`：组件变量，在 `--dt-*` 基础上定制特定组件外观
> - **统一要求**：文档示例统一使用 `--dt-*` 前缀，组件级变量使用 `--im-*`
> - **注意**：在组件样式中应优先使用上述实用类或直接引用 CSS 变量，避免写死 `#277EFB`、`8px` 等值。

---

## 5. AI 编码指令 (Styling Rules)

1. **严禁硬编码颜色或尺寸**：所有样式必须通过 CSS 变量或实用类实现。
2. **统一字体**：使用系统字体栈 `PingFang SC, Microsoft YaHei, system-ui`，不引入外部 Google Fonts。
3. **气泡对齐**：聊天气泡布局使用 `flex items-start`，确保头像与气泡顶部基线对齐。
4. **暗色模式适配**：通过 `data-theme="dark"` 自动切换变量，组件无需额外处理。
5. **全局滚动条**：在 `global.scss` 中统一样式，确保在暗色模式下颜色可见。

---

## 6. 维护与扩展指引

- **新增颜色**：在 `design-tokens.scss` 中添加 SCSS 变量并同步到 `variables.css`。
- **新增实用类**：在 `utilities.scss` 中使用 `@apply` 组合已有原子类，保持一致性。
- **图标扩展**：将 SVG 放入 `src/assets/icons/`，并在 `icon-index.md` 中登记。

---

## 7. 导航项样式规范 (Sidebar Navigation Item)

所有面板侧边栏的导航项（`.nav-item`）必须统一使用以下样式规范：

### 7.1 统一样式参数

```scss
.nav-item {
  display: flex;
  flex-direction: row;           // 水平排列：图标在前，文字在后
  align-items: center;            // 垂直居中
  gap: var(--dt-spacing-sm);     // 图标与文字间距 8px
  padding: var(--dt-spacing-sm) var(--dt-spacing-md);  // 上下8px 左右12px
  height: 40px;                   // 固定高度 40px
  box-sizing: border-box;        // 盒模型计算
  border-radius: var(--dt-radius-md);  // 圆角 8px
  cursor: pointer;
  transition: all var(--dt-transition-base);
}

/* 图标 */
.nav-icon {
  font-size: var(--dt-icon-size-md, 16px);  // 图标 16px
  flex-shrink: 0;
}

/* 文字 */
.nav-label {
  flex: 1;
  font-size: var(--dt-font-size-base, 14px);  // 文字 14px
  color: var(--dt-text-primary);
}

/* 悬浮态 */
.nav-item:hover {
  background: var(--dt-bg-session-hover);
}

/* 激活态 */
.nav-item.active {
  background: var(--dt-bg-session-active);
  color: var(--dt-brand-color);
  font-weight: var(--dt-font-weight-semibold);
}
```

### 7.2 已统一的面板

以下面板的导航样式已统一为此规范：

| 文件 | 组件 |
| :--- | :--- |
| `ContactsPanel.vue` | 通讯录侧边导航 |
| `TodoPanel.vue` | 待办侧边导航 |
| `DocumentsPanel.vue` | 云文档侧边导航 |
| `WorkbenchPanel.vue` | 工作台侧边导航 |

### 7.3 设计原则

- **固定高度**：`height: 40px` 确保所有导航项高度一致
- **水平布局**：`flex-direction: row` 确保图标在左、文字在右
- **使用 Design Token**：所有颜色、间距、圆角必须使用 `var(--dt-*)` 变量
- **统一激活态**：使用 `var(--dt-bg-session-active)` 背景 + 左侧蓝色指示条

---

> **业务背景**：本文件为前端全局样式的唯一权威来源，所有开发者在实现 UI 时必须遵循此规范，以保证视觉统一、易于维护，并避免因错误引用 Tailwind 而导致的样式冲突。
