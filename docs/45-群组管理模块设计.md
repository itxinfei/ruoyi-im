# 45 群组管理模块设计文档

> 用途：定义群组创建、成员管理、权限控制逻辑实现
> 更新时间：2026-03-21
> 优先级：P1

---

## 一、设计概述

### 1.1 职责边界
- **业务明细**：详见 [13-需求明细.md](13-需求明细.md) §7。
- **UI 强制规范 (C-005)**：客户端群详情入口必须统一归一为右侧抽屉 (**GroupDetailDrawer**)。
- **技术定位**：负责群组状态机（ACTIVE/DISBANDED）、成员角色（OWNER/ADMIN/MEMBER）变更及 WebSocket 同步。

---

## 二、后端实现 (JDK 1.8 规范)

### 2.1 核心成员校验逻辑

```java
@Service
@RequiredArgsConstructor
public class ImGroupServiceImpl implements ImGroupService {

    private final ImGroupMapper groupMapper;
    private final ImGroupMemberMapper memberMapper;

    /**
     * 校验用户是否有管理员权限
     * 遵循 JDK 1.8，严禁 var
     */
    public boolean isAdmin(Long groupId, Long userId) {
        ImGroupMember member = memberMapper.selectByGroupIdAndUserId(groupId, userId);
        if (Objects.isNull(member)) {
            return false;
        }
        String role = member.getRole();
        return "OWNER".equals(role) || "ADMIN".equals(role);
    }
}
```

---

## 三、前端实现 (纯 JS 规范)

### 3.1 群详情抽屉逻辑封装

```javascript
/**
 * 群详情逻辑封装 (纯 JS)
 * 强制要求：禁止使用 TypeScript
 */
import { ref, computed } from 'vue';
import { groupApi } from '@/api/group';

export function useGroupDetail(groupId) {
  const group = ref({});
  const members = ref([]);

  const loadGroup = async () => {
    const response = await groupApi.getDetail(groupId);
    group.value = response.data || {};
  };

  const loadMembers = async () => {
    const response = await groupApi.getMembers(groupId);
    members.value = response.list || [];
  };

  const isOwner = computed(() => {
    const currentUserId = 1; // 示例 ID
    return group.value.ownerId === currentUserId;
  });

  return {
    group,
    members,
    isOwner,
    loadGroup,
    loadMembers
  };
}
```

---

## 四、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-03-21 | 彻底肃清 TypeScript 语法，重构为纯 JS 逻辑，对齐 C 系列规则 | AI |
