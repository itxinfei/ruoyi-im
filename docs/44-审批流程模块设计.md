# 44 审批流程模块设计文档

> 用途：定义审批流程功能的技术实现方案
> 更新时间：2026-04-01
> 优先级：P1

---

## 一、设计概述

### 1.1 职责边界
- **需求参考**：本模块的业务逻辑、UI 布局及验收标准详见 [18-平台模块与管理后台需求.md](18-平台模块与管理后台需求.md) §1.3。
- **技术定位**：本设计文档专注于动态表单 JSON 解析、工作流引擎状态机及审批链路的后端实现。

### 1.2 核心逻辑模型
- **状态流转**：DRAFT -> PENDING -> APPROVED / REJECTED。
- **动态特性**：基于 `form_schema` 实现前端动态渲染，基于 `flow_config` 实现后端自动寻址。

---

## 二、数据库设计

### 2.1 审批模板表 (im_approval_template)

```sql
CREATE TABLE im_approval_template (
  id            BIGINT(20)    PRIMARY KEY AUTO_INCREMENT COMMENT '模板ID',
  name          VARCHAR(100)  NOT NULL COMMENT '模板名称',
  code          VARCHAR(50)   NOT NULL COMMENT '模板编码',
  category      VARCHAR(50)   NOT NULL COMMENT '类别: 请假/报销/出差/其他',
  description   VARCHAR(500)  NULL COMMENT '描述',
  form_schema   TEXT          NOT NULL COMMENT '表单配置(JSON)',
  flow_config   TEXT          NOT NULL COMMENT '流程配置(JSON)',
  icon          VARCHAR(200)  NULL COMMENT '图标',
  is_system     TINYINT(1)    DEFAULT 0 COMMENT '是否系统模板',
  status        VARCHAR(10)   DEFAULT 'ACTIVE' COMMENT '状态: ACTIVE/DISABLED',
  sort_order    INT(11)       DEFAULT 0 COMMENT '排序',
  create_by     VARCHAR(64)   NULL COMMENT '创建者',
  create_time   DATETIME      DEFAULT CURRENT_TIMESTAMP,
  update_by     VARCHAR(64)   NULL COMMENT '更新者',
  update_time   DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE INDEX uk_code (code),
  INDEX idx_category (category),
  INDEX idx_status (status)
) COMMENT '审批模板表';
```

---

## 三、后端实现 (JDK 1.8 规范)

### 3.1 审批流转核心逻辑

```java
/**
 * 审批工作流后端核心服务
 * 负责状态机流转与动态 JSON 表单路由跳转机制
 */
@Service
@RequiredArgsConstructor
public class ImApprovalServiceImpl implements ImApprovalService {

    private final ImApprovalMapper approvalMapper;

    /**
     * 将审批流转至下一个流向节点
     * 遵循纯正 JDK 1.8 规范，严防非法语法穿透
     *
     * @param approvalId 审批工单的唯一 ID (主键)
     */
    @Transactional(rollbackFor = Exception.class)
    public void flowToNextNode(Long approvalId) {
        ImApproval approval = approvalMapper.selectById(approvalId);
        if (Objects.isNull(approval)) {
            throw new BusinessException(500, "审批单不存在");
        }
        
        // 核心逻辑实现...
        approval.setStatus("APPROVED");
        approval.setUpdateTime(new Date());
        approvalMapper.updateById(approval);
    }
}
```

---

## 四、前端实现 (纯 JS 规范)

### 4.1 审批列表逻辑

```javascript
/**
 * 审批列表逻辑封装 (纯 JS)
 */
import { ref, onMounted } from 'vue';
import { approvalApi } from '@/api/approval';

export function useApprovalList() {
  const activeTab = ref('pending');
  const approvals = ref([]);

  const loadApprovals = async () => {
    // 接口返回 AjaxResult 结构
    const result = await approvalApi.getMyApprovals(activeTab.value);
    approvals.value = result.list || [];
  };

  const switchTab = (key) => {
    activeTab.value = key;
    loadApprovals();
  };

  return {
    activeTab,
    approvals,
    switchTab,
    loadApprovals
  };
}
```

---

## 五、UI 表现与对齐规范 (DingTalk Style)

### 5.1 审批首页布局 (Approval Home)

- **页面背景**：`#F2F4F5`
- **顶部搜索栏**：高度 `56px`，背景 `#FFFFFF`，内边距 `16px`
- **分类入口网格**：每行 4 个，图标 `40x40`，间距 `16px`

### 5.2 审批列表栏 (Approval List)

- **顶栏 Tabs**：
  - 高度 `48px`，背景 `#FFFFFF`
  - Tab 间距 `32px`，文字 `14px`
  - 激活态：文字 `#277EFB`，底边 `2px` 实线 `#277EFB`
  - 未激活态：文字 `rgba(23, 26, 29, 0.6)`

- **筛选栏**：
  - 高度 `40px`，内边距 `12px`
  - 下拉选择器宽度 `120px`，高度 `32px`

- **卡片项 (Card)**：
  - 高度：自适应，最小 `80px`
  - 内边距：`16px`
  - 背景：`#FFFFFF`
  - 边框：`1px solid rgba(23, 26, 29, 0.08)`
  - 圆角：`8px`
  - 间距：卡片之间 `12px`
  - Hover：背景变为 `#FAFAFA`，无阴影（钉钉扁平风格）

- **卡片内容布局**：
  - 左侧：图标 `40x40`，圆角 `8px`
  - 中间：标题（`14px`，粗体）+ 发起人（`12px`，灰色）+ 发起时间（`12px`，灰色）
  - 右侧：状态标签 + 操作按钮

### 5.3 状态标签 (Status Tags)

| 状态 | 背景色 | 文字色 | 尺寸 |
|-----|-------|-------|------|
| **待审批 (PENDING)** | `#FFF7E6` | `#FA8C16` | 高度 `22px`，宽度自适应，圆角 `4px` |
| **已通过 (APPROVED)** | `#F6FFED` | `#52C41A` | 同上 |
| **已拒绝 (REJECTED)** | `#FFF1F0` | `#FF4D4F` | 同上 |
| **已撤销 (CANCELLED)** | `#F5F5F5` | `rgba(23, 26, 29, 0.4)` | 同上 |
| **审批中 (PROCESSING)** | `#E6F2FF` | `#277EFB` | 同上 |

### 5.4 审批详情抽屉 (Detail Drawer)

- **尺寸**：宽度 `520px`
- **背景**：`#FFFFFF`
- **内边距**：`24px`

- **头部区域**：
  - 标题：`18px`，粗体，`#171A1D`
  - 状态标签：位于标题右侧
  - 分割线：下方 `1px solid rgba(23, 26, 29, 0.08)`

- **表单区域**：
  - 字段标签：`14px`，`rgba(23, 26, 29, 0.6)`
  - 字段值：`14px`，`#171A1D`
  - 字段间距：`16px`

- **流转时间线 (Timeline)**：
  - 左侧轴线：`2px` 宽，`#E5E7EB`
  - 节点圆点：直径 `12px`
    - 已完成：`#277EFB` 实心圆 + 白色勾选图标
    - 进行中：`#277EFB` 实心圆 + 呼吸动画    - 未开始：`#E5E7EB` 空心圆
  - 节点间距：`24px`
  - 节点文字：
    - 节点名称：`14px`，`#171A1D`
    - 操作人：`12px`，`rgba(23, 26, 29, 0.6)`
    - 操作时间：`12px`，`rgba(23, 26, 29, 0.4)`

- **底部操作栏**：
  - 高度：`64px`
  - 背景：`#FFFFFF`
  - 边框：顶部 `1px solid rgba(23, 26, 29, 0.08)`
  - 按钮：
    - 同意：背景 `#277EFB`，文字 `#FFFFFF`，宽度 `80px`
    - 拒绝：背景 `#FFFFFF`，文字 `#FF4D4F`，边框 `1px solid #FF4D4F`，宽度 `80px`
    - 转交：背景 `#FFFFFF`，文字 `#171A1D`，边框 `1px solid #E5E7EB`

### 5.5 发起审批弹窗

- **尺寸**：宽度 `600px`
- **表单类型选择**：
  - 网格布局，每行 4 个
  - 图标：`48x48`，圆角 `8px`
  - 文字：`14px`，居中
  - 选中态：边框 `2px solid #277EFB`，背景 `#E6F2FF`

---

## 六、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-04-01 | 统一品牌色为 #277EFB；补充审批卡片布局、状态标签尺寸、详情抽屉、时间线、发起弹窗规范 | AI |
| 2026-03-21 | 彻底肃清 TypeScript 语法，重构为纯 JS 逻辑 | AI |
| 2026-03-20 | 初始版本创建 | AI |