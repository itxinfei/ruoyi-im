# 48 待办任务模块设计文档

> 用途：定义待办管理、分类、提醒实现方案
> 更新时间：2026-04-01
> 优先级：P2

---

## 一、设计概述

### 1.1 职责边界
- **需求参考**：详见 [18-平台模块与管理后台需求.md](18-平台模块与管理后台需求.md) §1.4。
- **技术定位**：负责待办任务的状态机（PENDING/COMPLETED/EXPIRED）、优先级调度以及过期自动预警推送。

---

## 二、后端实现 (JDK 1.8 规范)

### 2.1 待办核心逻辑

```java
/**
 * 待办任务流转核心服务
 * 负责任务状态机的变更调度以及预警过期分配
 */
@Service
@RequiredArgsConstructor
public class ImTodoServiceImpl implements ImTodoService {

    private final ImTodoMapper todoMapper;

    /**
     * 更新用户的待办及流转状态
     * 遵循纯正 JDK 1.8 规范，严防非法语法穿透
     *
     * @param todoId 待办事项的全局唯一标识
     * @param status 目标枚举状态 (如 COMPLETED)
     */
    @Transactional(rollbackFor = Exception.class)
    public void updateStatus(Long todoId, String status) {
        ImTodo todo = todoMapper.selectById(todoId);
        if (Objects.nonNull(todo)) {
            todo.setStatus(status);
            todo.setUpdateTime(new Date());
            todoMapper.updateById(todo);
        }
    }
}
```

---

## 三、前端实现 (纯 JS 规范)

### 3.1 待办管理逻辑封装

```javascript
/**
 * 待办管理逻辑封装 (纯 JS)
 */
import { ref } from 'vue';
import { todoApi } from '@/api/todo';

export function useTodo() {
  const todoList = ref([]);

  const loadTodoList = async () => {
    const response = await todoApi.list();
    todoList.value = response.list || [];
  };

  const toggleComplete = async (todoId) => {
    await todoApi.toggle(todoId);
    await loadTodoList();
  };

  return {
    todoList,
    loadTodoList,
    toggleComplete
  };
}
```

---

## 四、UI 表现与对齐规范 (DingTalk Style)

### 4.1 待办页面整体布局 (Todo Layout)

- **页面背景**：`#F2F4F5`
- **左侧分类导航**：
  - 宽度：`200px`
  - 背景：`#FFFFFF`
  - 边框：右侧 `1px solid rgba(23, 26, 29, 0.08)`

- **右侧内容区**：
  - 背景：`#FFFFFF`
  - 内边距：`20px`
  - 最小宽度：`600px`

### 4.2 分类导航 (Category Navigation)

#### 4.2.1 导航项规范

- **高度**：固定 `40px`
- **内边距**：`12px 16px`
- **圆角**：`4px`
- **图标尺寸**：`16x16`
- **图标与文字间距**：`8px`

#### 4.2.2 导航项状态

| 状态 | 背景 | 文字色 | 附加样式 |
|-----|------|-------|---------|
| **默认** | `transparent` | `#171A1D` | 无 |
| **悬停** | `rgba(23, 26, 29, 0.04)` | `#171A1D` | 无 |
| **选中** | `#E6F2FF` | `#277EFB` | 左侧 `3px` 蓝色竖条 `#277EFB` |

#### 4.2.3 默认分类

| 分类 | 图标 | 图标色 |
|-----|------|-------|
| 全部待办 | `List` | `#ADB1B8` |
| 今天 | `CalendarToday` | `#277EFB` |
| 最近7天 | `DateRange` | `#52C41A` |
| 已完成 | `CheckCircle` | `rgba(23, 26, 29, 0.4)` |
| 已过期 | `WarningCircle` | `#FF4D4F` |

### 4.3 待办列表项 (Todo Item)

#### 4.3.1 列表项布局

- **高度**：自适应，最小 `48px`
- **内边距**：`12px 16px`
- **背景**：`#FFFFFF`
- **边框**：底边 `1px solid rgba(23, 26, 29, 0.08)`
- **Hover**：背景 `rgba(23, 26, 29, 0.02)`

#### 4.3.2 复选框交互 (Checkbox)

- **尺寸**：`16x16`
- **圆角**：`50%`（正圆）

| 状态 | 边框 | 背景 | 图标 |
|-----|------|------|------|
| **未完成** | `1.5px solid #ADB1B8` | `transparent` | 无 |
| **未完成 Hover** | `1.5px solid #277EFB` | `transparent` | 无 |
| **已完成** | `none` | `#277EFB` | 白色勾选 `10x10` |

#### 4.3.3 文字状态

| 状态 | 文字色 | 文字样式 |
|-----|-------|---------|
| **未完成** | `#171A1D` | 正常 |
| **已完成** | `rgba(23, 26, 29, 0.4)` | 中划线 |

### 4.4 优先级标签 (Priority Tags)

#### 4.4.1 标签样式

- **高度**：`20px`
- **内边距**：`0 8px`
- **圆角**：`4px`
- **字号**：`12px`

#### 4.4.2 优先级色值

| 优先级 | 背景色 | 文字色 | 边框 |
|-------|-------|-------|------|
| **高 (HIGH)** | `#FFF1F0` | `#FF4D4F` | `1px solid #FFCCC7` |
| **中 (MEDIUM)** | `#FFF7E6` | `#FA8C16` | `1px solid #FFD591` |
| **低 (LOW)** | `#E6F2FF` | `#277EFB` | `1px solid #91CAFF` |

### 4.5 截止时间状态 (Due Date Status)

| 状态 | 文字色 | 说明 |
|-----|-------|------|
| **正常** | `rgba(23, 26, 29, 0.6)` | 未到期 |
| **即将到期** | `#FA8C16` | 24小时内到期 |
| **已过期** | `#FF4D4F` | 已超过截止时间且未完成 |

### 4.6 新建待办弹窗 (Create Todo Dialog)

- **尺寸**：宽度 `480px`
- **内边距**：`24px`

#### 4.6.1 表单字段

| 字段 | 类型 | 高度 | 说明 |
|-----|------|------|------|
| 标题 | 输入框 | `36px` | 必填，最多 100 字符 |
| 截止时间 | 日期时间选择 | `36px` | 可选 |
| 优先级 | 单选按钮组 | - | 高/中/低，默认中 |
| 分类 | 下拉选择 | `36px` | 可选 |
| 备注 | 文本域 | `80px` | 可选 |
| 提醒 | 下拉选择 | `36px` | 准时/提前15分钟/提前1小时 |

#### 4.6.2 底部按钮

- **取消**：宽度 `80px`，背景 `#FFFFFF`，边框 `1px solid #E5E7EB`
- **创建**：宽度 `80px`，背景 `#277EFB`

### 4.7 待办详情抽屉 (Detail Drawer)

- **宽度**：`400px`
- **背景**：`#FFFFFF`
- **内边距**：`20px`

#### 4.7.1 内容区域

- **标题**：`18px`，粗体，`#171A1D`
- **状态行**：优先级标签 + 截止时间 + 分类标签
- **分隔线**：`1px solid rgba(23, 26, 29, 0.08)`
- **备注区**：`14px`，`#171A1D`，背景 `#F5F5F5`，圆角 `4px`
- **创建信息**：`12px`，`rgba(23, 26, 29, 0.4)`

#### 4.7.2 底部操作

- **完成按钮**：背景 `#277EFB`，宽度 `100%`
- **编辑/删除**：文字按钮，位于右上角

### 4.8 待办提醒通知

- **桌面通知**：
  - 标题：待办提醒
  - 内容：待办标题 + 截止时间
  - 操作按钮：[标记完成] [稍后提醒]

---

## 五、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-04-01 | 统一品牌色为 #277EFB；补充分类导航、优先级标签色值、截止时间状态、新建弹窗、详情抽屉规范 | AI |
| 2026-03-21 | 彻底肃清 TypeScript 语法，重构为纯 JS 逻辑 | AI |
