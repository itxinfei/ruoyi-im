<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.im.mapper.ImConversationMemberMapper">
    
    <resultMap type="com.ruoyi.im.domain.ImConversationMember" id="ImConversationMemberResult">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="userId" column="user_id"/>
        <result property="unreadCount" column="unread_count"/>
        <result property="lastReadMessageId" column="last_read_message_id"/>
        <result property="isPinned" column="is_pinned"/>
        <result property="isMuted" column="is_muted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectImConversationMemberVo">
        select id, conversation_id, user_id, unread_count, last_read_message_id, 
               is_pinned, is_muted, create_time, update_time 
        from im_conversation_member
    </sql>

    <select id="selectImConversationMemberById" parameterType="Long" resultMap="ImConversationMemberResult">
        <include refid="selectImConversationMemberVo"/>
        where id = #{id}
    </select>
    
    <select id="selectImConversationMemberList" parameterType="com.ruoyi.im.domain.ImConversationMember" resultMap="ImConversationMemberResult">
        <include refid="selectImConversationMemberVo"/>
        <where>
            <if test="conversationId != null"> and conversation_id = #{conversationId} </if>
            <if test="userId != null"> and user_id = #{userId} </if>
        </where>
    </select>

    <insert id="insertImConversationMember" parameterType="com.ruoyi.im.domain.ImConversationMember" useGeneratedKeys="true" keyProperty="id">
        insert into im_conversation_member
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="conversationId != null">conversation_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="unreadCount != null">unread_count,</if>
            <if test="lastReadMessageId != null">last_read_message_id,</if>
            <if test="isPinned != null">is_pinned,</if>
            <if test="isMuted != null">is_muted,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="conversationId != null">#{conversationId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="unreadCount != null">#{unreadCount},</if>
            <if test="lastReadMessageId != null">#{lastReadMessageId},</if>
            <if test="isPinned != null">#{isPinned},</if>
            <if test="isMuted != null">#{isMuted},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateImConversationMember" parameterType="com.ruoyi.im.domain.ImConversationMember">
        update im_conversation_member
        <trim prefix="SET" suffixOverrides=",">
            <if test="conversationId != null">conversation_id = #{conversationId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="unreadCount != null">unread_count = #{unreadCount},</if>
            <if test="lastReadMessageId != null">last_read_message_id = #{lastReadMessageId},</if>
            <if test="isPinned != null">is_pinned = #{isPinned},</if>
            <if test="isMuted != null">is_muted = #{isMuted},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteImConversationMemberById" parameterType="Long">
        delete from im_conversation_member where id = #{id}
    </delete>

    <delete id="deleteImConversationMemberByIds" parameterType="Long[]">
        delete from im_conversation_member where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <select id="selectImConversationMemberByConversationIdAndUserId" parameterType="map" resultMap="ImConversationMemberResult">
        select id, conversation_id, user_id, unread_count, last_read_message_id, 
               is_pinned, is_muted, create_time, update_time
        from im_conversation_member
        where conversation_id = #{conversationId} and user_id = #{userId}
    </select>
    
    <select id="selectImConversationMemberListByConversationId" parameterType="Long" resultMap="ImConversationMemberResult">
        select id, conversation_id, user_id, unread_count, last_read_message_id, 
               is_pinned, is_muted, create_time, update_time
        from im_conversation_member
        where conversation_id = #{conversationId}
    </select>
    
    <select id="selectImConversationMemberListByUserId" parameterType="Long" resultMap="ImConversationMemberResult">
        select id, conversation_id, user_id, unread_count, last_read_message_id, 
               is_pinned, is_muted, create_time, update_time
        from im_conversation_member
        where user_id = #{userId}
    </select>
    
    <delete id="deleteImConversationMemberByConversationIds" parameterType="Long[]">
        delete from im_conversation_member where conversation_id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>